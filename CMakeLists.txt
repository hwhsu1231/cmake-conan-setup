cmake_minimum_required(VERSION 3.21)
project(cmake-conan-setup LANGUAGES C CXX)
message("========== ${PROJECT_NAME} ==========")

# Optional Modules
include(cmake/modules/output-dirs.cmake)
include(cmake/modules/deploy-target.cmake)

# Find 'xxx-config.cmake' or 'FindXxx.cmake' generated by 'CMakeDeps' generator
find_package(fmt REQUIRED)
find_package(CLI11 REQUIRED)
find_package(Eigen3 REQUIRED)

add_subdirectory(source)